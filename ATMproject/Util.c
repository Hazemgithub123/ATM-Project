#include <stdio.h>
#include <string.h>

// Encrypt data using Hill Cipher (2x2 matrix)
void encryptData(char *input, char *output) {

    strcpy(output, input);
}

// Decrypt data using Hill Cipher (2x2 matrix)
void decryptData(char *input, char *output) {

    strcpy(output, input);
}

